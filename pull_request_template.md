# Add filters, sorting, and search to activities list

This pull request implements the following enhancements for issue #4:

- Adds a search box to filter activities by name or description
- Adds a sort dropdown to sort activities by name or available spots
- Updates the UI to make navigation easier as the list grows

## Changes
- Updated `index.html` to include new controls
- Updated `app.js` to implement client-side filtering and sorting

Closes #4
